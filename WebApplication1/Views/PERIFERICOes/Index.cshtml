@model IEnumerable<WebApplication1.Models.PERIFERICO>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_PaginaDiseno.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombre_periferico)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cant_disp)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.precio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MARCA.nombre_marca)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombre_periferico)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cant_disp)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.precio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MARCA.nombre_marca)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_PERIFERICO }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_PERIFERICO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_PERIFERICO })
        </td>
    </tr>
}

</table>
