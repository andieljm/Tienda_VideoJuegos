@model WebApplication1.Models.VIDEOJUEGO

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_PaginaDiseno.cshtml";
}

<h2 style="text-align:center">Eliminar Videojuego</h2>
<div>
    @Html.ActionLink("Volver", "Index")
</div>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <hr />
    <div class="formulario">
        <h3 style="color:white">¿Está seguro que desea eliminar?</h3>
        <br />
        <br />
        <div class="form_campo">
            @Html.DisplayNameFor(model => model.nombre_videojuego)
            <div class="form_eliminar">
                @Html.DisplayFor(model => model.nombre_videojuego)
            </div>
        </div>

        <div class="form_campo">
            @Html.DisplayNameFor(model => model.cant_disp)
            <div class="form_eliminar">
                @Html.DisplayFor(model => model.cant_disp)
            </div>
        </div>

        <div class="form_campo">
            @Html.DisplayNameFor(model => model.precio)
            <div class="form_eliminar">
                @Html.DisplayFor(model => model.precio)
            </div>
        </div>

        <div class="form_campo">
            @Html.DisplayNameFor(model => model.CONSOLA.nombre_consola)
            <div class="form_eliminar">
                @Html.DisplayFor(model => model.CONSOLA.nombre_consola)
            </div>
        </div>

        <div class="form_campo">
            <input type="submit" value="Eliminar" class="btn btn-danger" />
        </div>

    </div>

}
